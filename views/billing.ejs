<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Sistema de gestión de flotilla vehicular con control de seguros, mantenimientos y operadores">
    <meta name="theme-color" content="#001f3f">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="CRM Insurance">
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" type="image/png" href="/images/logo.png">
    <link rel="apple-touch-icon" href="/images/logo.png">
    <title>Facturación y Órdenes de Servicio | CRM Insurance System</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&family=Open+Sans:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #001f3f;
            --secondary: #87ceeb;
            --accent: #4da6ff;
            --light: #f5f7fa;
            --dark: #212529;
            --gray: #6c757d;
            --border: #dee2e6;
            --shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
        }
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Open Sans', sans-serif;
            background-color: #f5f7fa;
            color: var(--dark);
            line-height: 1.6;
        }
        header {
            background-color: white;
            border-bottom: 1px solid var(--border);
            padding: 0;
            margin-bottom: 0;
        }
        .header-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
        }
        .logo img {
            height: 40px;
            width: auto;
            object-fit: contain;
        }
        .logo-text {
            font-family: 'Montserrat', sans-serif;
            font-weight: 700;
            font-size: 1.5rem;
            color: var(--primary);
        }
        .logo-text span {
            color: var(--accent);
        }
        .user-menu {
            display: flex;
            align-items: center;
            gap: 12px;
            flex-wrap: wrap;
        }
        .btn {
            padding: 10px 20px;
            background-color: var(--primary);
            color: white;
            border: none;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 500;
            font-size: 0.9rem;
            cursor: pointer;
            transition: all 0.2s;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }
        .btn:hover {
            background-color: #003366;
        }
        .btn-secondary {
            background-color: #6c757d;
        }
        .btn-secondary:hover {
            background-color: #5a6268;
        }
        .btn-success {
            background-color: #28a745;
        }
        .btn-success:hover {
            background-color: #218838;
        }
        .btn-danger {
            background-color: #dc3545;
        }
        .btn-danger:hover {
            background-color: #c82333;
        }
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 30px 20px;
        }
        .page-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 24px;
            flex-wrap: wrap;
            gap: 16px;
        }
        .page-title {
            font-family: 'Montserrat', sans-serif;
            font-size: 2rem;
            font-weight: 600;
            color: var(--dark);
            margin-bottom: 4px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        .page-title i {
            color: var(--primary);
        }
        .page-subtitle {
            color: var(--gray);
            font-size: 0.9rem;
        }
        .layout {
            display: grid;
            grid-template-columns: minmax(0, 1.4fr) minmax(0, 1.2fr);
            gap: 20px;
            align-items: flex-start;
        }
        .section {
            background: white;
            border-radius: 8px;
            padding: 20px 18px;
            border: 1px solid var(--border);
        }
        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 16px;
        }
        .section-title {
            font-family: 'Montserrat', sans-serif;
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--dark);
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .section-title i {
            color: var(--primary);
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        thead {
            background-color: #f8f9fa;
        }
        th {
            padding: 10px 10px;
            text-align: left;
            font-weight: 600;
            font-size: 0.8rem;
            color: var(--dark);
            text-transform: uppercase;
            letter-spacing: 0.4px;
            border-bottom: 2px solid var(--border);
        }
        td {
            padding: 10px 10px;
            border-bottom: 1px solid var(--border);
            font-size: 0.9rem;
            color: var(--dark);
        }
        tbody tr:hover {
            background-color: #f8f9fa;
        }
        .badge {
            display: inline-block;
            padding: 4px 10px;
            border-radius: 999px;
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        .badge-success {
            background-color: #d4edda;
            color: #155724;
        }
        .badge-warning {
            background-color: #fff3cd;
            color: #856404;
        }
        .badge-danger {
            background-color: #f8d7da;
            color: #721c24;
        }
        .badge-info {
            background-color: #d1ecf1;
            color: #0c5460;
        }
        .text-muted {
            color: var(--gray);
            font-size: 0.8rem;
        }
        .font-weight-bold {
            font-weight: 700;
        }
        .empty-state {
            text-align: center;
            padding: 24px 12px;
            color: var(--gray);
        }
        .empty-state i {
            font-size: 2rem;
            color: #dee2e6;
            margin-bottom: 8px;
        }
        /* Modal */
        .modal {
            display: none;
            position: fixed;
            z-index: 1000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
        }
        .modal-content {
            background-color: white;
            margin: 5% auto;
            padding: 0;
            border-radius: 12px;
            width: 90%;
            max-width: 640px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
            max-height: 90vh;
            overflow-y: auto;
        }
        .modal-header {
            padding: 20px 22px;
            border-bottom: 1px solid var(--border);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .modal-header h2 {
            font-family: 'Montserrat', sans-serif;
            font-size: 1.4rem;
            font-weight: 600;
            color: var(--dark);
            margin: 0;
        }
        .modal-close {
            background: none;
            border: none;
            font-size: 1.5rem;
            color: var(--gray);
            cursor: pointer;
            padding: 0;
            width: 32px;
            height: 32px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 6px;
            transition: all 0.2s;
        }
        .modal-close:hover {
            background-color: #f8f9fa;
            color: var(--dark);
        }
        .modal-body {
            padding: 20px 22px 22px;
        }
        .form-group {
            margin-bottom: 14px;
        }
        .form-label {
            display: block;
            margin-bottom: 6px;
            font-weight: 600;
            color: var(--dark);
            font-size: 0.9rem;
        }
        .form-control {
            width: 100%;
            padding: 9px 12px;
            border: 1px solid var(--border);
            border-radius: 6px;
            font-size: 0.9rem;
            font-family: 'Open Sans', sans-serif;
            transition: border-color 0.2s;
        }
        .form-control:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 3px rgba(0, 31, 63, 0.1);
        }
        .form-row {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
            gap: 12px;
        }
        .form-actions {
            display: flex;
            gap: 10px;
            margin-top: 18px;
            padding-top: 16px;
            border-top: 1px solid var(--border);
        }
        @media (max-width: 992px) {
            .layout {
                grid-template-columns: 1fr;
            }
        }
        @media (max-width: 768px) {
            .header-container {
                flex-direction: column;
                gap: 10px;
                align-items: flex-start;
            }
            .page-header {
                flex-direction: column;
                align-items: flex-start;
            }
            .user-menu {
                width: 100%;
                justify-content: flex-start;
            }
            .btn {
                font-size: 0.85rem;
            }
            table {
                font-size: 0.82rem;
            }
        }
        @media (max-width: 576px) {
            .container {
                padding: 24px 14px;
            }
            .page-title {
                font-size: 1.6rem;
            }
            th, td {
                padding: 8px 6px;
            }
        }
        
        /* Responsive Styles - Mobile & Tablet */
        @media (max-width: 768px) {
            .header-container {
                flex-direction: column;
                gap: 15px;
                padding: 15px;
            }
            .logo-text {
                font-size: 1.2rem;
            }
            .user-menu {
                width: 100%;
                justify-content: space-between;
                flex-wrap: wrap;
            }
            .user-menu a, .user-menu button {
                font-size: 0.85rem;
                padding: 8px 12px;
            }
            .container {
                padding: 15px 10px;
            }
            .page-header {
                flex-direction: column;
                align-items: flex-start;
                gap: 15px;
            }
            .page-title {
                font-size: 1.5rem;
            }
            .page-header .btn {
                width: 100%;
                justify-content: center;
            }
            .tabs {
                flex-wrap: wrap;
                gap: 8px;
            }
            .tabs button {
                flex: 1;
                min-width: 120px;
                font-size: 0.85rem;
                padding: 10px 12px;
            }
            table {
                display: block;
                overflow-x: auto;
                -webkit-overflow-scrolling: touch;
            }
            table thead {
                display: none;
            }
            table tbody {
                display: block;
                width: 100%;
            }
            table tr {
                display: block;
                margin-bottom: 15px;
                border: 1px solid var(--border);
                border-radius: 8px;
                padding: 15px;
                background: white;
            }
            table td {
                display: block;
                text-align: right;
                padding: 8px 0;
                border: none;
                border-bottom: 1px solid #f0f0f0;
            }
            table td:last-child {
                border-bottom: none;
            }
            table td:before {
                content: attr(data-label);
                float: left;
                font-weight: 600;
                color: var(--primary);
            }
            .modal-content {
                width: 95%;
                margin: 10% auto;
                max-height: 85vh;
            }
            .modal-header {
                padding: 15px;
            }
            .modal-header h2 {
                font-size: 1.2rem;
            }
            .modal-body {
                padding: 15px;
            }
            .form-actions {
                flex-direction: column;
            }
            .form-actions .btn {
                width: 100%;
            }
            td > div {
                display: flex;
                flex-direction: column;
                gap: 8px;
            }
            .btn-attachments, .btn-history, .btn-danger {
                font-size: 0.75rem;
                padding: 6px 10px;
            }
        }
        
        @media (max-width: 480px) {
            .logo-text {
                font-size: 1rem;
            }
            .user-menu {
                flex-direction: column;
                width: 100%;
            }
            .user-menu a, .user-menu button {
                width: 100%;
                text-align: center;
            }
            .page-title {
                font-size: 1.3rem;
            }
            .container {
                padding: 10px 5px;
            }
            .tabs button {
                font-size: 0.8rem;
                padding: 8px 10px;
            }
            table td {
                font-size: 0.85rem;
            }
            .modal-content {
                width: 100%;
                margin: 0;
                border-radius: 0;
                max-height: 100vh;
            }
        }
    </style>
</head>
<body>
    <header>
        <div class="header-container">
            <div class="logo">
                <img src="/images/logo.png" alt="Logo">
                <div class="logo-text">CRM <span>Insurance</span> System</div>
            </div>
            <div class="user-menu">
                <span class="text-muted"><%= user.nombre || user.username %></span>
                <a href="/dashboard" class="btn">Dashboard</a>
                <a href="/vehicles" class="btn">Vehículos</a>
                <a href="/logout" class="btn btn-secondary">Cerrar Sesión</a>
            </div>
        </div>
    </header>

    <div class="container">
        <div class="page-header">
            <div>
                <h1 class="page-title">
                    <i class="fas fa-file-invoice-dollar"></i>
                    Facturación y Órdenes de Servicio
                </h1>
                <p class="page-subtitle">Control de costos por vehículo y siniestro sin cambiar tu operación actual.</p>
            </div>
            <div style="display:flex; gap:10px; flex-wrap:wrap;">
                <button class="btn btn-success" id="btnNewOrder">
                    <i class="fas fa-plus"></i> Nueva Orden de Servicio
                </button>
                <button class="btn btn-success" id="btnNewInvoice">
                    <i class="fas fa-file-invoice"></i> Nueva Factura
                </button>
            </div>
        </div>

        <div class="layout">
            <!-- Órdenes de servicio -->
            <div class="section">
                <div class="section-header">
                    <h2 class="section-title">
                        <i class="fas fa-clipboard-list"></i>
                        Órdenes de Servicio Recientes
                    </h2>
                </div>
                <% if (!orders || orders.length === 0) { %>
                <div class="empty-state">
                    <i class="fas fa-clipboard-list"></i>
                    <p>No hay órdenes de servicio registradas.</p>
                </div>
                <% } else { %>
                <div style="overflow-x:auto;">
                    <table>
                        <thead>
                            <tr>
                                <th>Fecha</th>
                                <th>Vehículo</th>
                                <th>Tipo</th>
                                <th>Descripción</th>
                                <th>Estado</th>
                                <th>Total</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% orders.forEach(function(order) { %>
                            <tr>
                                <td data-label="Fecha"><%= new Date(order.fecha).toLocaleDateString('es-ES') %></td>
                                <td data-label="Vehículo">
                                    <% if (order.vehicle_id) { %>
                                        <strong>#<%= order.numero_vehiculo %></strong>
                                        <div class="text-muted"><%= order.marca %> <%= order.modelo %></div>
                                    <% } else { %>
                                        <span class="text-muted">Sin vehículo</span>
                                    <% } %>
                                </td>
                                <td data-label="Tipo"><%= order.tipo || '-' %></td>
                                <td class="text-muted" style="max-width: 260px;" data-label="Descripción">
                                    <%= order.descripcion || '-' %>
                                </td>
                                <td data-label="Estado">
                                    <% 
                                    const estado = order.estado || 'Abierta';
                                    let cls = 'badge-info';
                                    if (estado === 'Abierta') cls = 'badge-warning';
                                    if (estado === 'En Proceso') cls = 'badge-info';
                                    if (estado === 'Cerrada') cls = 'badge-success';
                                    if (estado === 'Cancelada') cls = 'badge-danger';
                                    %>
                                    <span class="badge <%= cls %>"><%= estado %></span>
                                </td>
                                <td data-label="Total">
                                    <% if (order.total != null) { %>
                                        <span class="font-weight-bold">
                                            $<%= Number(order.total).toLocaleString('es-MX', { minimumFractionDigits: 2 }) %>
                                        </span>
                                        <span class="text-muted"><%= order.moneda || 'MXN' %></span>
                                    <% } else { %>
                                        <span class="text-muted">-</span>
                                    <% } %>
                                </td>
                                <td data-label="Acciones">
                                    <div style="display: flex; gap: 6px; flex-wrap: wrap;">
                                        <button class="btn btn-attachments" style="padding: 4px 10px; font-size: 0.8rem; background-color: var(--accent);" data-entity-id="<%= order.id %>" data-entity-type="service_order" data-entity-name="Orden #<%= order.id %>">
                                            <i class="fas fa-paperclip"></i>
                                        </button>
                                        <button class="btn btn-history" style="padding: 4px 10px; font-size: 0.8rem; background-color: #6c757d;" data-entity-id="<%= order.id %>" data-entity-type="service_order" data-entity-name="Orden #<%= order.id %>">
                                            <i class="fas fa-history"></i>
                                        </button>
                                        <button class="btn btn-danger btn-delete-order" style="padding: 4px 10px; font-size: 0.8rem;" data-order-id="<%= order.id %>">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
                <% } %>
            </div>

            <!-- Facturas -->
            <div class="section">
                <div class="section-header">
                    <h2 class="section-title">
                        <i class="fas fa-file-invoice"></i>
                        Facturas Recientes
                    </h2>
                </div>
                <% if (!invoices || invoices.length === 0) { %>
                <div class="empty-state">
                    <i class="fas fa-file-invoice"></i>
                    <p>No hay facturas registradas.</p>
                </div>
                <% } else { %>
                <div style="overflow-x:auto;">
                    <table>
                        <thead>
                            <tr>
                                <th>Fecha</th>
                                <th>Folio</th>
                                <th>Cliente</th>
                                <th>Orden</th>
                                <th>Estado</th>
                                <th>Total</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% invoices.forEach(function(inv) { %>
                            <tr>
                                <td data-label="Fecha"><%= new Date(inv.fecha).toLocaleDateString('es-ES') %></td>
                                <td class="font-weight-bold" data-label="Folio">
                                    <%= inv.serie ? inv.serie + '-' : '' %><%= inv.folio || '-' %>
                                </td>
                                <td data-label="Cliente">
                                    <%= inv.cliente_nombre || '-' %>
                                    <% if (inv.cliente_rfc) { %>
                                        <div class="text-muted"><%= inv.cliente_rfc %></div>
                                    <% } %>
                                </td>
                                <td data-label="Orden">
                                    <% if (inv.service_order_id) { %>
                                        <span class="text-muted">OS #<%= inv.service_order_id %></span>
                                        <% if (inv.order_tipo) { %>
                                            <div class="text-muted"><%= inv.order_tipo %></div>
                                        <% } %>
                                    <% } else { %>
                                        <span class="text-muted">-</span>
                                    <% } %>
                                </td>
                                <td data-label="Estado">
                                    <% 
                                    const est = inv.estado || 'Borrador';
                                    let icls = 'badge-info';
                                    if (est === 'Borrador') icls = 'badge-warning';
                                    if (est === 'Emitida') icls = 'badge-success';
                                    if (est === 'Cancelada') icls = 'badge-danger';
                                    %>
                                    <span class="badge <%= icls %>"><%= est %></span>
                                </td>
                                <td data-label="Total">
                                    <% if (inv.total != null) { %>
                                        <span class="font-weight-bold">
                                            $<%= Number(inv.total).toLocaleString('es-MX', { minimumFractionDigits: 2 }) %>
                                        </span>
                                        <span class="text-muted"><%= inv.moneda || 'MXN' %></span>
                                    <% } else { %>
                                        <span class="text-muted">-</span>
                                    <% } %>
                                </td>
                                <td data-label="Acciones">
                                    <div style="display: flex; gap: 6px; flex-wrap: wrap;">
                                        <button class="btn btn-attachments" style="padding: 4px 10px; font-size: 0.8rem; background-color: var(--accent);" data-entity-id="<%= inv.id %>" data-entity-type="invoice" data-entity-name="Factura <%= inv.folio || inv.id %>">
                                            <i class="fas fa-paperclip"></i>
                                        </button>
                                        <button class="btn btn-history" style="padding: 4px 10px; font-size: 0.8rem; background-color: #6c757d;" data-entity-id="<%= inv.id %>" data-entity-type="invoice" data-entity-name="Factura <%= inv.folio || inv.id %>">
                                            <i class="fas fa-history"></i>
                                        </button>
                                        <button class="btn btn-danger btn-delete-invoice" style="padding: 4px 10px; font-size: 0.8rem;" data-invoice-id="<%= inv.id %>">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                            <% }); %>
                        </tbody>
                    </table>
                </div>
                <% } %>
            </div>
        </div>
    </div>

    <!-- Modal Orden de Servicio -->
    <div id="orderModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2>Nueva Orden de Servicio</h2>
                <button class="modal-close" onclick="window.closeOrderModal()">&times;</button>
            </div>
            <div class="modal-body">
                <form id="orderForm">
                    <div class="form-row">
                        <div class="form-group">
                            <label class="form-label">Fecha *</label>
                            <input type="date" name="fecha" class="form-control" required>
                        </div>
                        <div class="form-group">
                            <label class="form-label">Vehículo</label>
                            <select name="vehicle_id" class="form-control">
                                <option value="">Sin vehículo</option>
                                <% vehicles.forEach(function(v) { %>
                                <option value="<%= v.id %>">#<%= v.numero_vehiculo %> - <%= v.marca %> <%= v.modelo %></option>
                                <% }); %>
                            </select>
                        </div>
                        <div class="form-group">
                            <label class="form-label">Tipo</label>
                            <select name="tipo" class="form-control">
                                <option value="">Seleccionar</option>
                                <option value="Mantenimiento">Mantenimiento</option>
                                <option value="Reparación">Reparación</option>
                                <option value="Siniestro">Siniestro</option>
                                <option value="Otro">Otro</option>
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="form-label">Descripción</label>
                        <textarea name="descripcion" rows="3" class="form-control" placeholder="Descripción del servicio realizado o por realizar"></textarea>
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label class="form-label">Estado</label>
                            <select name="estado" class="form-control">
                                <option value="Abierta">Abierta</option>
                                <option value="En Proceso">En Proceso</option>
                                <option value="Cerrada">Cerrada</option>
                                <option value="Cancelada">Cancelada</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label class="form-label">Total</label>
                            <input type="number" name="total" class="form-control" step="0.01" min="0" placeholder="0.00">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Moneda</label>
                            <select name="moneda" class="form-control">
                                <option value="MXN">MXN</option>
                                <option value="USD">USD</option>
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="form-label">Notas</label>
                        <textarea name="notas" rows="2" class="form-control" placeholder="Notas internas, relación con póliza o siniestro, etc."></textarea>
                    </div>
                    <div class="form-actions">
                        <button type="button" class="btn btn-secondary" onclick="window.closeOrderModal()">Cancelar</button>
                        <button type="submit" class="btn btn-success">
                            <i class="fas fa-save"></i> Guardar Orden
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <!-- Modal Factura -->
    <div id="invoiceModal" class="modal">
        <div class="modal-content">
            <div class="modal-header">
                <h2>Nueva Factura</h2>
                <button class="modal-close" onclick="window.closeInvoiceModal()">&times;</button>
            </div>
            <div class="modal-body">
                <form id="invoiceForm">
                    <div class="form-row">
                        <div class="form-group">
                            <label class="form-label">Fecha *</label>
                            <input type="date" name="fecha" class="form-control" required>
                        </div>
                        <div class="form-group">
                            <label class="form-label">Serie</label>
                            <input type="text" name="serie" class="form-control" placeholder="Ej: A">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Folio</label>
                            <input type="text" name="folio" class="form-control" placeholder="Ej: 1234">
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="form-label">Cliente (Nombre/Razón Social)</label>
                        <input type="text" name="cliente_nombre" class="form-control" placeholder="Nombre del cliente">
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label class="form-label">RFC</label>
                            <input type="text" name="cliente_rfc" class="form-control" placeholder="RFC">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Orden de Servicio (ID)</label>
                            <input type="number" name="service_order_id" class="form-control" min="1" placeholder="Opcional">
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label class="form-label">Subtotal</label>
                            <input type="number" name="subtotal" class="form-control" step="0.01" min="0" placeholder="0.00">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Impuestos</label>
                            <input type="number" name="impuestos" class="form-control" step="0.01" min="0" placeholder="0.00">
                        </div>
                        <div class="form-group">
                            <label class="form-label">Total</label>
                            <input type="number" name="total" class="form-control" step="0.01" min="0" placeholder="0.00">
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label class="form-label">Moneda</label>
                            <select name="moneda" class="form-control">
                                <option value="MXN">MXN</option>
                                <option value="USD">USD</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label class="form-label">Estado</label>
                            <select name="estado" class="form-control">
                                <option value="Borrador">Borrador</option>
                                <option value="Emitida">Emitida</option>
                                <option value="Cancelada">Cancelada</option>
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="form-label">Notas</label>
                        <textarea name="notas" rows="2" class="form-control" placeholder="Notas o comentarios sobre la factura"></textarea>
                    </div>
                    <div class="form-actions">
                        <button type="button" class="btn btn-secondary" onclick="window.closeInvoiceModal()">Cancelar</button>
                        <button type="submit" class="btn btn-success">
                            <i class="fas fa-save"></i> Guardar Factura
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <script>
        // Modales Órdenes - Funciones globales
        window.openOrderModal = function() {
            var modal = document.getElementById('orderModal');
            if (modal) {
                modal.style.display = 'block';
            }
        };
        
        window.closeOrderModal = function() {
            var modal = document.getElementById('orderModal');
            var form = document.getElementById('orderForm');
            if (modal) {
                modal.style.display = 'none';
            }
            if (form) {
                form.reset();
            }
        };

        // Modales Facturas - Funciones globales
        window.openInvoiceModal = function() {
            var modal = document.getElementById('invoiceModal');
            if (modal) {
                modal.style.display = 'block';
            }
        };
        
        window.closeInvoiceModal = function() {
            var modal = document.getElementById('invoiceModal');
            var form = document.getElementById('invoiceForm');
            if (modal) {
                modal.style.display = 'none';
            }
            if (form) {
                form.reset();
            }
        };

        // Event listeners para botones de crear
        document.addEventListener('DOMContentLoaded', function() {
            var btnNewOrder = document.getElementById('btnNewOrder');
            if (btnNewOrder) {
                btnNewOrder.addEventListener('click', function() {
                    window.openOrderModal();
                });
            }

            var btnNewInvoice = document.getElementById('btnNewInvoice');
            if (btnNewInvoice) {
                btnNewInvoice.addEventListener('click', function() {
                    window.openInvoiceModal();
                });
            }
        });

        window.addEventListener('click', function (event) {
            const orderModal = document.getElementById('orderModal');
            const invoiceModal = document.getElementById('invoiceModal');
            if (event.target === orderModal) {
                window.closeOrderModal();
            }
            if (event.target === invoiceModal) {
                window.closeInvoiceModal();
            }
        });

        // Envío formularios
        document.getElementById('orderForm').addEventListener('submit', async function (e) {
            e.preventDefault();
            const formData = new FormData(this);
            const data = Object.fromEntries(formData);

            if (data.total) data.total = parseFloat(data.total);

            try {
                const res = await fetch('/api/service-orders', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(data)
                });
                const result = await res.json();
                if (result.success) {
                    alert('Orden de servicio creada correctamente');
                    window.location.reload();
                } else {
                    alert('Error: ' + (result.error || 'No se pudo crear la orden'));
                }
            } catch (err) {
                alert('Error de conexión: ' + err.message);
            }
        });

        document.getElementById('invoiceForm').addEventListener('submit', async function (e) {
            e.preventDefault();
            const formData = new FormData(this);
            const data = Object.fromEntries(formData);

            if (data.service_order_id === '') data.service_order_id = null;
            if (data.subtotal) data.subtotal = parseFloat(data.subtotal);
            if (data.impuestos) data.impuestos = parseFloat(data.impuestos);
            if (data.total) data.total = parseFloat(data.total);

            try {
                const res = await fetch('/api/invoices', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify(data)
                });
                const result = await res.json();
                if (result.success) {
                    alert('Factura creada correctamente');
                    window.location.reload();
                } else {
                    alert('Error: ' + (result.error || 'No se pudo crear la factura'));
                }
            } catch (err) {
                alert('Error de conexión: ' + err.message);
            }
        });

        // Funciones para adjuntos e historial
        let currentEntityId = null;
        let currentEntityType = null;

        function openAttachmentsModal(entityId, entityType, entityName) {
            currentEntityId = entityId;
            currentEntityType = entityType;
            document.getElementById('attachmentsModalTitle').textContent = 'Adjuntos - ' + entityName;
            document.getElementById('attachmentsModal').style.display = 'block';
            loadAttachments();
        }

        function openHistoryModal(entityId, entityType, entityName) {
            currentEntityId = entityId;
            currentEntityType = entityType;
            document.getElementById('historyModalTitle').textContent = 'Historial - ' + entityName;
            document.getElementById('historyModal').style.display = 'block';
            loadHistory();
        }

        async function loadAttachments() {
            if (!currentEntityId || !currentEntityType) return;
            
            try {
                const response = await fetch('/api/attachments/' + currentEntityType + '/' + currentEntityId);
                const data = await response.json();
                
                const container = document.getElementById('attachmentsList');
                if (data.success && data.attachments.length > 0) {
                    container.innerHTML = data.attachments.map(function(att) {
                        return '<div class="attachment-item" style="display: flex; justify-content: space-between; align-items: center; padding: 12px; border: 1px solid var(--border); border-radius: 6px; margin-bottom: 10px;"><div style="flex: 1;"><div style="font-weight: 600; margin-bottom: 4px;"><i class="fas fa-file"></i> ' + att.nombre_original + '</div><div style="font-size: 0.85rem; color: var(--gray);">' + (att.tamano / 1024).toFixed(2) + ' KB • ' + (att.categoria || 'otro') + ' • ' + new Date(att.created_at).toLocaleDateString('es-ES') + '</div></div><div style="display: flex; gap: 8px;"><a href="/api/attachments/' + att.id + '/download" class="btn" style="padding: 6px 12px; font-size: 0.85rem;"><i class="fas fa-download"></i></a><button onclick="deleteAttachment(' + att.id + ')" class="btn btn-danger" style="padding: 6px 12px; font-size: 0.85rem;"><i class="fas fa-trash"></i></button></div></div>';
                    }).join('');
                } else {
                    container.innerHTML = '<p class="text-muted">No hay adjuntos aún</p>';
                }
            } catch (error) {
                console.error('Error cargando adjuntos:', error);
            }
        }

        async function loadHistory() {
            if (!currentEntityId || !currentEntityType) return;
            
            try {
                const response = await fetch('/api/history/' + currentEntityType + '/' + currentEntityId);
                const data = await response.json();
                
                const container = document.getElementById('historyList');
                if (data.success && data.history.length > 0) {
                    container.innerHTML = data.history.map(function(item) {
                        return '<div class="history-item" style="padding: 12px; border-left: 3px solid var(--accent); margin-bottom: 12px; background-color: #f8f9fa; border-radius: 4px;"><div style="display: flex; justify-content: space-between; margin-bottom: 6px;"><span style="font-weight: 600;">' + item.accion + '</span><span style="font-size: 0.85rem; color: var(--gray);">' + new Date(item.created_at).toLocaleString('es-ES') + '</span></div><div style="font-size: 0.9rem; color: var(--dark); margin-bottom: 4px;">' + (item.descripcion || '') + '</div><div style="font-size: 0.8rem; color: var(--gray);">Por: ' + (item.usuario_nombre || item.username) + '</div></div>';
                    }).join('');
                } else {
                    container.innerHTML = '<p class="text-muted">No hay historial aún</p>';
                }
            } catch (error) {
                console.error('Error cargando historial:', error);
            }
        }

        async function deleteAttachment(attachmentId) {
            if (!confirm('¿Estás seguro de eliminar este adjunto?')) return;
            
            try {
                const response = await fetch('/api/attachments/' + attachmentId, {
                    method: 'DELETE'
                });
                
                if (response.ok) {
                    loadAttachments();
                } else {
                    alert('Error al eliminar adjunto');
                }
            } catch (error) {
                console.error('Error eliminando adjunto:', error);
                alert('Error al eliminar adjunto');
            }
        }

        async function deleteServiceOrder(orderId) {
            if (!confirm('¿Estás seguro de eliminar esta orden de servicio?')) return;
            
            try {
                const response = await fetch('/api/service-orders/' + orderId, {
                    method: 'DELETE'
                });
                
                const data = await response.json();
                if (data.success) {
                    alert('Orden de servicio eliminada');
                    window.location.reload();
                } else {
                    alert('Error: ' + (data.error || 'No se pudo eliminar'));
                }
            } catch (error) {
                console.error('Error eliminando orden:', error);
                alert('Error al eliminar orden de servicio');
            }
        }

        async function deleteInvoice(invoiceId) {
            if (!confirm('¿Estás seguro de eliminar esta factura?')) return;
            
            try {
                const response = await fetch('/api/invoices/' + invoiceId, {
                    method: 'DELETE'
                });
                
                const data = await response.json();
                if (data.success) {
                    alert('Factura eliminada');
                    window.location.reload();
                } else {
                    alert('Error: ' + (data.error || 'No se pudo eliminar'));
                }
            } catch (error) {
                console.error('Error eliminando factura:', error);
                alert('Error al eliminar factura');
            }
        }

        // Event listeners para botones
        document.addEventListener('DOMContentLoaded', function() {
            // Botones de adjuntos
            document.querySelectorAll('.btn-attachments').forEach(function(btn) {
                btn.addEventListener('click', function() {
                    const entityId = this.getAttribute('data-entity-id');
                    const entityType = this.getAttribute('data-entity-type');
                    const entityName = this.getAttribute('data-entity-name');
                    openAttachmentsModal(entityId, entityType, entityName);
                });
            });

            // Botones de historial
            document.querySelectorAll('.btn-history').forEach(function(btn) {
                btn.addEventListener('click', function() {
                    const entityId = this.getAttribute('data-entity-id');
                    const entityType = this.getAttribute('data-entity-type');
                    const entityName = this.getAttribute('data-entity-name');
                    openHistoryModal(entityId, entityType, entityName);
                });
            });

            // Botones de eliminar orden
            document.querySelectorAll('.btn-delete-order').forEach(function(btn) {
                btn.addEventListener('click', function() {
                    const orderId = parseInt(this.getAttribute('data-order-id'));
                    deleteServiceOrder(orderId);
                });
            });

            // Botones de eliminar factura
            document.querySelectorAll('.btn-delete-invoice').forEach(function(btn) {
                btn.addEventListener('click', function() {
                    const invoiceId = parseInt(this.getAttribute('data-invoice-id'));
                    deleteInvoice(invoiceId);
                });
            });
        });

        // Manejar subida de archivos
        var uploadForm = document.getElementById('uploadAttachmentForm');
        if (uploadForm) {
            uploadForm.addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const formData = new FormData();
            const fileInput = document.getElementById('attachmentFile');
            const descripcion = document.getElementById('attachmentDescription').value;
            const categoria = document.getElementById('attachmentCategory').value;
            
            if (!fileInput.files[0]) {
                alert('Por favor selecciona un archivo');
                return;
            }
            
            formData.append('file', fileInput.files[0]);
            formData.append('entity_type', currentEntityType);
            formData.append('entity_id', currentEntityId);
            formData.append('descripcion', descripcion);
            formData.append('categoria', categoria);
            
            try {
                const response = await fetch('/api/attachments', {
                    method: 'POST',
                    body: formData
                });
                
                const data = await response.json();
                
                if (data.success) {
                    alert('Archivo subido exitosamente');
                    document.getElementById('uploadAttachmentForm').reset();
                    loadAttachments();
                } else {
                    alert('Error: ' + (data.error || 'Error al subir archivo'));
                }
            } catch (error) {
                console.error('Error subiendo archivo:', error);
                alert('Error al subir archivo');
            }
            });
        }
    </script>

    <!-- Modal de Adjuntos -->
    <div id="attachmentsModal" class="modal">
        <div class="modal-content" style="max-width: 800px;">
            <div class="modal-header">
                <h2 id="attachmentsModalTitle">Adjuntos</h2>
                <button class="modal-close" onclick="document.getElementById('attachmentsModal').style.display='none'">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <div class="modal-body">
                <h3 style="margin-bottom: 15px; font-size: 1.1rem;">Subir Nuevo Archivo</h3>
                <form id="uploadAttachmentForm" style="margin-bottom: 30px; padding-bottom: 20px; border-bottom: 2px solid var(--border);">
                    <div class="form-group">
                        <label class="form-label">Archivo *</label>
                        <input type="file" id="attachmentFile" class="form-control" required accept="image/*,.pdf,.doc,.docx,.xls,.xlsx,.txt">
                    </div>
                    <div class="form-group">
                        <label class="form-label">Categoría</label>
                        <select id="attachmentCategory" class="form-control">
                            <option value="foto_siniestro">Foto de Siniestro</option>
                            <option value="poliza_pdf">Póliza PDF</option>
                            <option value="documento_vehiculo">Documento de Vehículo</option>
                            <option value="factura">Factura</option>
                            <option value="otro">Otro</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label class="form-label">Descripción</label>
                        <textarea id="attachmentDescription" class="form-control" rows="2" placeholder="Descripción del archivo"></textarea>
                    </div>
                    <button type="submit" class="btn btn-success">
                        <i class="fas fa-upload"></i> Subir Archivo
                    </button>
                </form>
                
                <h3 style="margin-bottom: 15px; font-size: 1.1rem;">Archivos Adjuntos</h3>
                <div id="attachmentsList"></div>
            </div>
        </div>
    </div>

    <!-- Modal de Historial -->
    <div id="historyModal" class="modal">
        <div class="modal-content" style="max-width: 800px;">
            <div class="modal-header">
                <h2 id="historyModalTitle">Historial</h2>
                <button class="modal-close" onclick="document.getElementById('historyModal').style.display='none'">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <div class="modal-body">
                <div id="historyList"></div>
            </div>
        </div>
    </div>
</body>
</html>


